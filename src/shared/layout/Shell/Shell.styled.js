import styled from 'styled-components';

export const Wrapper = styled.div`
    display: block;
    padding: 4rem;
`;

export const Page = styled.div`
    display: block;
    position: relative;
    overflow: hidden;
`;